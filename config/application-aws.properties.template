# AWS RDS Configuration Template
# Copy this file to application-aws.properties and fill in your values
# DO NOT commit application-aws.properties to git

# Database Configuration
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=3306
DB_NAME=event_streaming_db
DB_USERNAME=admin
DB_PASSWORD=your-secure-password-here

# AWS Configuration
AWS_REGION=us-east-1

# Optional: Override default settings
# MYSQL_USE_SSL=true
# MYSQL_MAX_CONNECTIONS=10
# MYSQL_MIN_CONNECTIONS=2